#! /usr/bin/env bash

set -Eeo pipefail

base_image="pauletaylor/rapids-ide:ucx-gcc${GCC_VERSION:-9}-cuda${CUDA_VERSION_MAJOR:-11}.${CUDA_VERSION_MINOR:-6}.${CUDA_VERSION_PATCH:-2}-${LINUX_DISTRO:-ubuntu20.04}"

run-in-docker-build \
    --repo=cumlprims_mg \
    --base_image="$base_image" \
    -- configure-cumlprims_mg-cpp
