#! /usr/bin/env bash

set -Eeo pipefail

rm -rf ${RMM_CPP_BINARY_DIR:-$workspace/rmm/build};
if [[ -d "${RMM_CPP_SOURCE_DIR:-$workspace/rmm}/build" ]]; then
    find "${RMM_CPP_SOURCE_DIR:-$workspace/rmm}/build" -type d -empty -delete;
fi
