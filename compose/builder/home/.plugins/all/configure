#! /usr/bin/env bash

set -Eeo pipefail

$(which configure-rmm-cpp) && configure-rmm-cpp || true;
$(which configure-cudf-cpp) && configure-cudf-cpp || true;
$(which configure-raft-cpp) && configure-raft-cpp || true;
$(which configure-cumlprims_mg-cpp) && configure-cumlprims_mg-cpp || true;
$(which configure-cuml-cpp) && configure-cuml-cpp || true;
$(which configure-cugraph-ops-cpp) && configure-cugraph-ops-cpp || true;
$(which configure-cugraph-cpp) && configure-cugraph-cpp || true;
$(which configure-cuspatial-cpp) && configure-cuspatial-cpp || true;
